# Base URL of the deployed backend (Express + Socket.IO), no trailing slash.
# Local dev:
VITE_SERVER_URL=http://localhost:4000

# Production example:
# VITE_SERVER_URL=https://your-backend.onrender.com
